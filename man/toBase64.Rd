% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{toBase64}
\alias{toBase64}
\title{Encode a numeric vector as a base64 Float32 payload}
\usage{
toBase64(vec)
}
\arguments{
\item{vec}{Numeric vector. \code{NULL} is returned unchanged.}
}
\value{
Character string of the form \code{"base64:..."} or \code{NULL}.
}
\description{
Serialises a numeric vector as little-endian 32-bit floats and prefixes the
result with \code{"base64:"} so the companion JavaScript widget recognises it as
a binary buffer rather than a JSON array. Using this transfer path keeps
the payload size at ~25\% of the equivalent JSON representation.
}
\examples{
toBase64(c(1, 2, 3.5))
}
