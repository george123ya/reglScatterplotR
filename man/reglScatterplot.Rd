% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scatterplot.R
\name{reglScatterplot}
\alias{reglScatterplot}
\title{Interactive WebGL scatterplot}
\usage{
reglScatterplot(
  data = NULL,
  x,
  y,
  colorBy = NULL,
  groupBy = NULL,
  assay = NULL,
  filterBy = NULL,
  pointSize = NULL,
  opacity = NULL,
  pointColor = NULL,
  categoricalPalette = "Set1",
  continuousPalette = "viridis",
  customPalette = NULL,
  customColors = NULL,
  pointLabels = NULL,
  xlab = "X",
  ylab = "Y",
  title = NULL,
  legendTitle = NULL,
  xrange = NULL,
  yrange = NULL,
  rangePadding = 0.15,
  vmin = NULL,
  vmax = NULL,
  centerZero = FALSE,
  showAxes = TRUE,
  showTooltip = TRUE,
  backgroundColor = NULL,
  axisColor = "#333333",
  legendBg = "#ffffff",
  legendText = "#000000",
  legendPosition = "top-right",
  draggableLegend = TRUE,
  width = NULL,
  height = NULL,
  enableDownload = FALSE,
  plotId = NULL,
  syncPlots = NULL,
  elementId = NULL,
  dataVersion = NULL,
  masterId = NULL,
  autoFit = FALSE,
  margins = NULL,
  fontSize = 12,
  legendFontSize = 12,
  filteredIndices = NULL,
  selectedIndices = NULL,
  syncState = TRUE
)
}
\arguments{
\item{data}{Optional \code{data.frame} (or any object that supports \code{[[}).
When supplied, \code{x}, \code{y}, \code{colorBy}, \code{groupBy} and the columns named in
\code{filterBy} are looked up by name in \code{data}. When \code{NULL}, these arguments
must be vectors of matching length.}

\item{x, y}{Either column names (when \code{data} is non-NULL) or numeric vectors
giving point coordinates. For \code{SingleCellExperiment} / \code{SpatialExperiment}
inputs, pass the \code{reducedDim} name as \code{x} (e.g. \code{"UMAP"}); \code{y} is
ignored. Use \code{"spatial"} for tissue coordinates on a \code{SpatialExperiment}.}

\item{colorBy}{Optional column name or vector used to colour points.
Character/factor input is treated as categorical, numeric as continuous.}

\item{groupBy}{Optional column name or vector. Used by the legend to expose
a second categorical filter that intersects with \code{colorBy}.}

\item{assay}{Name of the assay to read when \code{colorBy} names a feature
row of an \code{SCE} (default \code{"logcounts"}, falls back to assay #1).
Ignored for plain data frames.}

\item{filterBy}{Optional \code{data.frame} of additional numeric covariates that
the JavaScript widget exposes for range filtering.}

\item{pointSize, opacity}{Numeric. When \code{NULL}, sensible defaults are picked
based on the number of points (\code{pointSize = 3} and \code{opacity = 0.8} for
small data; \code{pointSize = 1}, \code{opacity = 1} once \code{n > 500000}).}

\item{pointColor}{Optional fixed hex colour. When given, overrides
\code{colorBy}.}

\item{categoricalPalette, continuousPalette}{Names of fallback palettes
(a \code{RColorBrewer} name and a \code{viridisLite} name respectively).}

\item{customPalette, customColors}{Optional explicit palettes. \code{customColors}
is a named character vector mapping level -> hex; \code{customPalette} is an
unnamed vector used in level order.}

\item{pointLabels}{Optional character vector aligned to the points; shown
in the hover tooltip. Use this for gene names, cell barcodes, etc.}

\item{xlab, ylab, title, legendTitle}{Axis/labels.}

\item{xrange, yrange}{Optional length-2 numeric vectors fixing the axes
instead of using the data range. When supplied, they bypass the
automatic padding from \code{rangePadding}.}

\item{rangePadding}{Fractional padding added to each side of the data
range when \code{xrange} / \code{yrange} aren't supplied (default \code{0.1} =
10\% on each side, matching the \code{ggplot2} convention). Set to \code{0}
to draw exactly the data range.}

\item{vmin, vmax}{Continuous-colour clipping. Accepts a number, \code{"min"},
\code{"max"}, or a percentile string like \code{"p99"}.}

\item{centerZero}{Logical. If \code{TRUE}, the continuous colour scale is forced
to be symmetric around zero (useful for log-fold-change displays).}

\item{showAxes, showTooltip}{Logical toggles.}

\item{backgroundColor, axisColor, legendBg, legendText}{Hex colour overrides
for cosmetic styling.}

\item{legendPosition}{Starting position of the legend. One of
\code{"top-right"} (default), \code{"top-left"}, \code{"bottom-right"}, \code{"bottom-left"},
or a length-2 numeric vector \code{c(x, y)} giving absolute pixel offsets
from the top-left of the plot.}

\item{draggableLegend}{Logical; when \code{TRUE} (default), the legend can be
dragged with the mouse. Set to \code{FALSE} to pin it to \code{legendPosition}.}

\item{width, height}{Widget dimensions (any valid CSS unit). When \code{NULL},
\code{htmlwidgets} picks defaults appropriate to the host context.}

\item{enableDownload}{Logical. Show the download (PNG/SVG/PDF) button.
Defaults to \code{FALSE} because the button is unreliable inside IDE iframes
(RStudio Viewer, VSCode Jupyter), where browser download dialogs and
\code{html2canvas}/\code{jsPDF} script injections often fail. Set to \code{TRUE}
explicitly for standalone HTML files or Shiny apps; even then the
button is hidden automatically when the widget is running inside an
IDE iframe.}

\item{plotId}{Optional string identifying the plot. Required when linking
plots together via \code{\link[=enableReglScatterplotSync]{enableReglScatterplotSync()}}.}

\item{syncPlots}{Optional character vector of plot ids to sync with.}

\item{elementId}{DOM id for the containing div (passed to \code{htmlwidgets}).}

\item{dataVersion}{Optional integer/string used by the JS layer to detect
when the underlying data has changed.}

\item{masterId}{Optional id of another plot whose camera should seed this
one's view (used when a plot is added to an already-synced group).}

\item{autoFit}{Logical. When \code{TRUE}, the camera zooms to fit the data
region exactly on initial render.}

\item{margins}{Optional \code{list(top, right, bottom, left)} of pixel margins.}

\item{fontSize, legendFontSize}{Numeric font sizes in pixels.}

\item{filteredIndices, selectedIndices}{Optional 0-based integer vectors
for server-driven filtering / selection.}

\item{syncState}{Logical. When \code{FALSE}, the widget starts with global sync
disabled.}
}
\value{
An \code{htmlwidgets} object of class \code{"reglScatterplot"}.
}
\description{
Renders a \code{regl-scatterplot} widget capable of displaying millions of
points in the browser. Works in standalone HTML, the RStudio Viewer,
Shiny applications and Jupyter notebooks (via IRkernel).
}
\section{Bioconductor data structures}{

When \code{data} is a \code{SingleCellExperiment} or \code{SpatialExperiment}, the
function reroutes through a helper that pulls coordinates from
\code{reducedDim()} (e.g. \code{dimred = "UMAP"}) - or from \code{spatialCoords()} when
\code{dimred = "spatial"} for a \code{SpatialExperiment}. \code{colorBy} and \code{groupBy}
may then name either a \code{colData} column or a feature in \code{rownames(sce)}
(in the latter case \code{assay()} is read - \code{"logcounts"} by default, or the
assay named by the \code{assay} argument).
}

\examples{
set.seed(1L)
df <- data.frame(
    x = rnorm(2000),
    y = rnorm(2000),
    group = sample(letters[1:4], 2000, replace = TRUE),
    score = runif(2000)
)
reglScatterplot(df, x = "x", y = "y", colorBy = "group")
reglScatterplot(df, x = "x", y = "y", colorBy = "score",
                continuousPalette = "magma")

\donttest{
# Single-cell UMAP from a SingleCellExperiment
# reglScatterplot(sce, dimred = "UMAP", colorBy = "cluster")
# Colour by a gene
# reglScatterplot(sce, dimred = "UMAP", colorBy = "MS4A1")
# Spatial coordinates from a SpatialExperiment
# reglScatterplot(spe, dimred = "spatial", colorBy = "celltype")
}

}
\seealso{
\code{\link[=enableReglScatterplotSync]{enableReglScatterplotSync()}}, \code{\link[=updateReglPointSize]{updateReglPointSize()}}
}
